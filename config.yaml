name:
  model_name: "yolo_v11"

model:
  name: yolo11n
  experiment: development

data_count: 
  old: Palm Tree Label 200m Splitted.v7i.yolov11
  root: data_count
  sampling: sampling
  train_path: train
  test_path: test
  valid_path: valid
  num_classes: 1
  names: Palm-Tree

data_classify: 
  old: acfr-fruit-dataset
  root: data_classify
  sampling: sampling_classify
  train_path: train
  test_path: test
  valid_path: valid
  num_classes: 1
  names: Palm-Tree

output:
  root: output
  clean: clean
  model: model

train:
  test_size: 0.2
  random_state: 42
  shuffle: true
  batch_size: 8
  num_workers: 4
  max_epochs: 1

preprocessing:
  resize_img: 640

